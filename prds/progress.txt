# Ralph Progress Log
Started: Sun Mar 29 14:49:00 EEST 2026
---

## Codebase Patterns
- MongoService methods return `Result<T>` — `{ ok: true, data: T } | { ok: false, error: string }`
- IPC handlers use `wrap()` helper for automatic error catching; export handler won't use wrap (needs dialog/stream lifecycle)
- Pre-existing web typecheck errors for missing `loadHistory`, `saveHistory`, `clearHistory` on MongoApi type (US-003 will fix)
- Pre-existing lint error in ConnectionManager.tsx (react-hooks/set-state-in-effect) and button.tsx warning
- Pre-existing test errors: base-ui `getAnimations` not a function in Sidebar.test.tsx (uncaught exceptions, not test failures)
- BSON import: `import { BSON } from 'bson'` — use `BSON.serialize(doc)` for raw buffer output
- Preload `MongoApi` type is `typeof api` in index.ts; index.d.ts has a separate manual interface for global `window.api`
- Export progress type `ExportProgress` lives in `src/shared/types.ts` — reuse in renderer components

---

## 2026-03-29 - US-001
- Added `exportCollection(dbName, collName, output, onProgress, signal)` method to MongoService
- Uses `BSON.serialize(doc)` per document (raw: true had typing issues as predicted)
- Backpressure handled via `drain` event, progress throttled at 200ms intervals
- Abort signal checked each iteration, cursor closed on abort
- Files changed: `src/main/mongo-service.ts`
- **Learnings for future iterations:**
  - Node typecheck (`typecheck:node`) passes clean; web typecheck has pre-existing errors
  - `BSON.serialize()` returns `Uint8Array` which is compatible with Node `Writable.write()`
---

## 2026-03-29 - US-002
- Added `mongo:export-collection` IPC handler: save dialog → gzip stream → progress events → cleanup
- Added `mongo:cancel-export` IPC handler: AbortController lookup and abort
- Active exports tracked via `Map<string, AbortController>` keyed by `db.collection`
- Cleanup handles both success (flush gzip/file) and error (destroy streams, unlink partial file)
- Files changed: `src/main/ipc-handlers.ts`, `src/main/ipc-handlers.test.ts`
- **Learnings for future iterations:**
  - Handler does NOT use `wrap()` — needs dialog + stream + progress lifecycle beyond simple try/catch
  - `gzip.end(cb)` must be called to flush, then wait for file `finish` event before cleanup
  - Test for "already in progress": use a controllable promise for `exportCollection`, flush microtasks with `setTimeout(r, 10)`, then resolve to avoid timeout
  - Pre-existing uncaught exceptions in Sidebar.test.tsx (`getAnimations`) and lint errors in ConnectionDialog.tsx are not from this work
---

## 2026-03-29 - US-003
- Added `exportCollection`, `cancelExport`, `onExportProgress` to preload api object
- Added `ExportProgress` type to `src/shared/types.ts`
- Updated `MongoApi` interface in `index.d.ts` with export methods + missing `loadHistory`, `saveHistory`, `clearHistory`
- `onExportProgress` strips `IpcRendererEvent` before calling callback, returns cleanup function
- Files changed: `src/shared/types.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`
- **Learnings for future iterations:**
  - `index.d.ts` declares global `window.api` type separately from the inferred `MongoApi` in `index.ts` — both must be kept in sync
  - `ExportProgress` type is shared — import from `../shared/types` in renderer components
  - All typechecks now pass clean (both node and web)
---

## 2026-03-29 - US-004
- Added CollectionRow component with download icon, progress bar, and cancel button
- Download icon appears on hover (opacity-0 group-hover/coll:opacity-100)
- During export: row background fills with linear-gradient based on count/coll.count progress
- During export: icon changes to X (always visible), clicking cancels export
- Toast shown on complete (success) or error
- Double-click safe: handleExport returns early if already exporting
- Progress state is local useState in CollectionRow, not in store
- useEffect subscribes to onExportProgress, cleans up on unmount
- Updated Sidebar.test.tsx with export mock methods (onExportProgress, exportCollection, cancelExport)
- Files changed: `src/renderer/src/components/Sidebar.tsx`, `src/renderer/src/components/Sidebar.test.tsx`
- **Learnings for future iterations:**
  - Each CollectionRow subscribes to onExportProgress and filters by db+collection name
  - `group-hover/coll` pattern works for nested hover targets within Button components
  - Sidebar tests need all window.api methods mocked — adding new API methods requires updating mock
  - Pre-existing test errors (`getAnimations`) are unhandled errors, not test failures — all 61 tests pass
---
