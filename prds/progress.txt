# Ralph Progress Log
Started: Sun Mar 22 16:50:13 EET 2026

## Codebase Patterns
- No eslint config in project; typecheck (`npx tsc --noEmit`) is the main quality gate
- Shadcn-style UI components in `src/renderer/src/components/ui/`
- Store pattern: Zustand store at `src/renderer/src/store.ts`
- Table wrapper div in `table.tsx` no longer has `overflow-auto` — scroll handled by DocumentTable outer div
- Uses pnpm (not npm) for package management

---

## 2026-03-22 - US-001
- Implemented sticky headers and zebra striping
- Files changed: `src/renderer/src/components/ui/table.tsx`, `src/renderer/src/components/DocumentTable.tsx`
- **Learnings for future iterations:**
  - table.tsx wrapper div had `overflow-auto` removed so sticky positioning resolves against DocumentTable's outer scroll container
  - Zebra striping uses Tailwind `even:bg-muted/30` on TableRow
  - TableHeader gets `sticky top-0 z-10 bg-background` class
---

## 2026-03-22 - US-002
- Implemented table-layout fixed, ellipsis truncation, and resizable columns
- Files changed: `src/renderer/src/components/DocumentTable.tsx`
- **Learnings for future iterations:**
  - `formatCell` no longer truncates at 100 chars — CSS `truncate` class handles ellipsis
  - Column widths tracked in `columnWidths` state, reset via useEffect when `columnsKey` changes
  - Resize handle is a 4px div at right edge of th, uses document-level mousemove/mouseup
  - `user-select: none` set on body during drag, cleared on mouseup
  - Resize handle `onClick` has `e.stopPropagation()` to avoid triggering sort (future US-004)
---

## 2026-03-22 - US-003
- Installed `@radix-ui/react-popover`, created shadcn-style `popover.tsx` wrapper
- Added expand icon (Maximize2) on cell hover, opens Radix Popover with full value + Copy button
- Files changed: `src/renderer/src/components/ui/popover.tsx` (new), `src/renderer/src/components/DocumentTable.tsx`, `package.json`, `pnpm-lock.yaml`
- **Learnings for future iterations:**
  - Uses pnpm, not npm
  - Popover follows same shadcn pattern as dialog.tsx
  - `toast.success()` for positive feedback, `toast.error()` for errors
  - Cell `group` class + `opacity-0 group-hover:opacity-100` for hover reveal of expand icon
---

## 2026-03-22 - US-004
- Added `sort` state and `setSort` action to store; cycles asc->desc->null
- Sort passed in all find() calls (fetchPage, selectCollection, runQuery, refreshDocs)
- Sort cleared on selectCollection and runQuery
- Sort headers with ArrowUp/ArrowDown/ArrowUpDown icons; disabled in aggregate mode
- Files changed: `src/renderer/src/store.ts`, `src/renderer/src/components/DocumentTable.tsx`
- **Learnings for future iterations:**
  - Backend already supported sort in FindOpts — just needed wiring through store
  - `setSort` calls `fetchPage(0)` after updating state to trigger refetch
  - Sort icons use conditional rendering based on queryMode === 'aggregate'
---

## 2026-03-22 - US-005
- Added `setLimit` action to store (resets skip to 0, refetches)
- Reworked pagination bar: [Prev] [Page <input> of Y] [rows/page <select>] [Next]
- Page input applies on Enter/blur, clamps to valid range
- Page size select: 10, 20, 50, 100
- Files changed: `src/renderer/src/store.ts`, `src/renderer/src/components/DocumentTable.tsx`
- **Learnings for future iterations:**
  - `pageInput` state synced via useEffect on `currentPage` to stay in sync with store
  - Select uses native HTML element for simplicity (no shadcn select component needed)
---
